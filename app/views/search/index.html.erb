<div class="flex flex-col items-center justify-center p-6 sm:p-10 space-y-6 relative h-screen w-screen">
  <header class="flex flex-col space-y-6 md:space-y-0 md:flex-row justify-between my-3 px-5">
      <h2 class="text-4xl font-semibold"><%=pluralize(@employees.count, "Result") %></h2>
  </header>
  <main class="flex flex-col overflow-y-auto px-5 py-3">
    <article class="grid md:grid-cols-2 xl:grid-cols-4 gap-6 py-6" >
      <%= render 'employees/employees'%>
    </article>
  </main>
</div>

